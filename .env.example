# Copy the variable into your shell environment. Never commit a real key.
ZAI_API_KEY=replace-with-your-key

# Optional overrides
LOCALDECK_MODEL=glm-5.2
LOCALDECK_BASE_URL=https://open.bigmodel.cn/api/paas/v4/
LOCALDECK_RUNS_DIR=./runs

